# Calcular a circunferencia de um circulo

import math as mt

raio = float(input("Coloque o tamanho do raio de um circulo "))
Circun = 2 * mt.pi * raio

print(f"O tamanho da circunferencia do circulo e de: {round(Circun, 2)}")