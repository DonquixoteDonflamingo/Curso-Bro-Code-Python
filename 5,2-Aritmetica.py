x = 3.14
y = 4
z = 5

# resultado = round(x) # Arredonda uma variavel
# resultado = abs(y) # Valor absoluto = Distancia de um numero do
# resultado = pow(y, 3) # exponenciaçãp
# resultado = max(x, y, z) # Acha qual é o número maior entre diferentes números
# resultado = min(x, y, z) # Acha o menor número

print(resultado)